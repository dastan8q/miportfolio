/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placehold.co', 'i.pinimg.com', 'cdn.builder.io', 'assets.cdn.builder.io'], 
    },
};

export default nextConfig;